.build
.ci-cache
.git
.github
.depot
.gitignore
.cursor
*.md
Docs
# Tests/ must stay in the build context: SwiftPM still loads the AppTests target during
# `swift build --product App`; without Tests/AppTests present, SPM can mis-resolve sources and
# report "overlapping sources" with Sources/App.
.dockerignore
Dockerfile
docker-compose.yml
.env
.env.*
**/.DS_Store
Thumbs.db
